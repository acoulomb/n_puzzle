package main

import "fmt"

func manhattan_heuristic(base, end [][]int) int {
	solve := 0
	for i := 0; i < len(base); i++ {
		for r := 0; r < len(base); r++ {
			fmt.Println("bof")
		}
	}
	return solve
}
